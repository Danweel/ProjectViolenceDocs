[build-system]
requires = ["poetry-core>=1.8.0", "setuptools", "wheel"]
build-backend = "poetry.core.masonry.api, setuptools.build_meta"

# -----------------------------------------------------------------
# PEP 621 metadata
# -----------------------------------------------------------------
[project]
name = "projectviolencedocs"
version = "0.1.0"
description = "Documentation for Project Violence"
readme = "README.md"
license = "MIT"
# licence files = ["LICENSE"]  #OPTIONAL, points to a licence file
authors = [
    {name = "Danweel", email = "daniil.woodland@pm.me"},
    {name = "Community"}          # optional second author
]

# ---- core runtime dependencies (inside the same [project] table) ----
requires-python = ">=3.9"   # Once this line is present, the resolver no longer falls back to the historic “>=2.7,<2.8” default.

# dependencies = [
#    "sphinx>=7.2",
#    "sphinx-rtd-theme>=2.0,<3.0",
#    "readthedocs-build>=2.0"
#]
# Not needed here if Poetry is handling them (see the Yaml)
# -----------------------------------------------------------------
# Poetry Groups
# -----------------------------------------------------------------
[tool.poetry]
package-mode = false   # ← tells Poetry not to expect a Python package
packages = []          # ← no Python package to install

[tool.poetry.extras]   # ← this defines the dependancies to install, then 'dependancy groups' below define the minimum requirements for each item.
docs = ["sphinx", "sphinx-rtd-theme", "readthedocs-build"]  
dev = ["sphinx", "sphinx-rtd-theme", "readthedocs-build", "pytest", "ruff"]


# -----------------------------------------------------------------
# Poetry Dependency Groups
# -----------------------------------------------------------------
[tool.poetry.group.docs.dependencies]  # use poetry install --with docs for regular contributors (light, strips out unnecessary tools)
sphinx = "^7.2"                        # because these are defined in the Core dependencies, this isn't strictly needed, but I'm using it in the setup script for clarity.
sphinx-rtd-theme = "^2.0"
readthedocs-build = "^2.0"

[tool.poetry.group.dev.dependencies]   # replaces deprecated [dev-dependencies], use poetry install --with dev to install everything required to maintain the Read the Docs project.
sphinx = "^7.2"
sphinx-rtd-theme = "^2.0"
readthedocs-build = "^2.0"
pytest = "^8.0"                        # I think optional in our case, but I'm including it for now until I'm sure we don't need it.
ruff = "^0.6"           

# For now they are roughly the same except the linter, but additional tools I find I need on the dev end will be added eventually (if necessary). 
# This is just a note about how to set installation options for now.


# -----------------------------------------------------------------
# OPTIONAL: keep the old Poetry block for tools that still expect it
# -----------------------------------------------------------------
# If you want Poetry to continue reading the dependencies from its
# legacy section, add them to `project.dynamic` and leave the block
# unchanged.  Otherwise you can delete the whole block.
# We aren't doing this, but I'm leaving this instruction here in case we need it later.